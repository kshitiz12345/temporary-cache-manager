{
  "name": "temporary-cache-manager",
  "version": "1.0.2",
  "description": "Temporary cache manager is in memory caching management system where data is cached in heap memory",
  "main": "cache.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kshitiz Agarwal",
  "license": "ISC",
  "dependencies": {}
}
